<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.GuardianAlbum" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/brand_orange</item>
        <item name="colorPrimaryDark">@color/brand_orange_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/brand_orange_dark</item>
        <item name="android:windowBackground">@color/white</item>
    </style>

    <!-- 앱 시작 시 로고 스플래시 -->
    <style name="Theme.GuardianAlbum.Launch" parent="Theme.GuardianAlbum">
        <item name="android:windowBackground">@drawable/splash</item>
    </style>
</resources>
